"use strict";

const SysAppClientTokenModel = require("../models/sys-app-client-token-model.js");

class SysAppClientTokenController {
  initKeys(ucEnv) {
    return SysAppClientTokenModel.initKeys(ucEnv.uri.getAwid());
  }

  listKeys(ucEnv) {
    return SysAppClientTokenModel.listKeys(ucEnv.uri.getAwid());
  }
}

module.exports = new SysAppClientTokenController();
