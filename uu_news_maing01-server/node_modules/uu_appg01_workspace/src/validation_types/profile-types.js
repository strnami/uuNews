/* eslint-disable */
const sysSetProfileDtoInType = shape({
  code:code().isRequired(),
  roleUri:uri().isRequired()
});

const getProfileDtoInType = shape({
  code: code().isRequired()
});

const sysListProfilesDtoInType = shape({
  codeList: oneOf([code(), array(code(), 32)]),
  pageInfo: shape({
    pageIndex: integer(0, null),
    pageSize: integer(0, null)
  })
});

const sysUpdateProfilesDtoInType = shape({
  awidList: array(hexa32Code(), 1, null)
})