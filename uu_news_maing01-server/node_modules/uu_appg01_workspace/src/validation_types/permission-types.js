/* eslint-disable */
const createPermissionDtoInType = shape({
  profileCode: code().isRequired(),
  uuIdentityList: oneOf([uuIdentity(), array(uuIdentity(), 1, 10)]).isRequired(),
  ttl: integer()
})

const sysDeletePermissionDtoInType = shape({
  profileCode: code().isRequired(),
  uuIdentityList: oneOf([uuIdentity(), array(uuIdentity(), 1, 1024)]).isRequired()
})

const sysListPermissionsDtoInType = shape({
  profileCodeList: oneOf([code(), array(code(), 32)]),
  uuIdentityList: oneOf([uuIdentity(), array(uuIdentity(), 1024)]),
  pageInfo: shape({
    pageIndex: integer(0, null),
    pageSize: integer(0, null)
  })
})
